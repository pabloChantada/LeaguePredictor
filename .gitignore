.venv/
__pycache__/
*.pyc
.pytest_cache/

# Dataset: ~9 GB of raw JSON, not versioned
riot_dataset/

# Trained models: regenerable with train.py, not versioned...
models/*.joblib
models/*.pt
# ...except the one served live (140 KB). It goes into the repo on purpose: live
# mode needs no API key, so with this the repo works right after cloning.
!models/baseline_model.joblib
.env
.coverage
coverage.xml
.devcontainer/