# Main pipeline
requests
python-dotenv
pandas
numpy
# Pinned: the repo ships models/baseline_model.joblib, and an sklearn pickle
# loaded with a different version warns (InconsistentVersionWarning) or fails.
scikit-learn==1.9.0
joblib
matplotlib

# Live dashboard
streamlit

# Only for experiments/train_lstm.py (shelved). Install separately with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch
